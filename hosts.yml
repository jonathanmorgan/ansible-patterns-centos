all:
  hosts:
    research.local
  children:
    new_servers:
      hosts:
        research.local
    miniconda3_servers:
      hosts:
        resesarch.local
    scl_python3_servers:
      hosts:
        resesarch.local
    apache_servers:
      hosts:
        research.local
    mod_wsgi_servers:
      hosts:
        research.local
    postgres_servers:
      hosts:
        research.local
    jupyterhub_servers:
      hosts:
        research.local
    r_servers:
      hosts:
        research.local
    rstudio_servers:
      hosts:
        research.local
    django_servers:
      hosts:
        research.local
    sourcenet_servers:
      hosts:
        research.local
    mariadb_servers:
      hosts:
        research.local
